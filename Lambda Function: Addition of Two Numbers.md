# Lambda Function in Python: Addition of Two Numbers

## 🎯 Aim
To write a Python program that defines a **lambda function** which takes two arguments `a` and `b`, and returns their sum.

## 🧠 Algorithm
1. Get two integer inputs from the user.
2. Use a **lambda function** to define a function `f` that returns `a + b`.
3. Call the function with the user inputs and print the result.

## 🧾 Program
```
add = lambda a, b: a + b
a = int(input())
b = int(input())
print("Enter the first integer:",a)
print("Enter the second integer :",b)
print("The Result of a+b is:",add(a, b))
```
## Output

<img width="1703" height="386" alt="image" src="https://github.com/user-attachments/assets/4efd0041-6f26-48f0-9713-265c4941bb6c" />

## Result

Thus, the program has been executed successfully.
